interface:
  display_name: "Protometer"
  short_description: "Repo-specific patterns and workflows for protometer"
  default_prompt: "Use the protometer repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
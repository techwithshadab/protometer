# Python
.venv/
__pycache__/
*.pyc
# Node
node_modules/
frontend/dist/
# Crawled + built data (regenerated by the ingest step)
data/raw/
data/processed/
data/index/
# Local env
.env
# Embedding-model cache
.cache/

# Runtime reveal-registry (SQLite DB of role-gated detokenization records + its WAL/SHM);
# generated at runtime, holds operational state, not source. (Mirrors the AML data/audit/ exclusion.)
data/reveal-registry.db
data/reveal-registry.db-shm
data/reveal-registry.db-wal
data/reveal-registry.json

global:
  scrape_interval: 15s
scrape_configs:
  - job_name: pushgateway
    honor_labels: true   # keep the job/scope labels the ingest push set
    static_configs:
      - targets: ["pushgateway:9091"]
